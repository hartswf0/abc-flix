# SWARMS

This directory encapsulates the **Latent Choreography and Flow Console** prototypes for the Operative Architecture.

## Contents
It hosts tools like point-cloud vector flows, latent variable controllers, NSDS policy trainers, and spatial configuration operators that compose the abstract data modeling dimension of the ICARO generative suite.

For an immediate visual overview of all interfaces in this folder, open `index.html`.
